name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger with approval

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://atlasinvestor.com  # Update with your production URL
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to production server
        # This is a placeholder - you'll need to configure based on your hosting
        run: |
          echo "Deploying to production..."
          echo "Configure this step based on your hosting provider"
          # Example for SSH deployment:
          # ssh user@production-server "cd /opt/atlas-investor && git pull && docker-compose up -d --build"


